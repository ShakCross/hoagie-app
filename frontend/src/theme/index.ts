import COLORS from './colors';
import { FONTS, TYPOGRAPHY } from './typography';
import { SPACING, getSpacing, SHADOWS } from './spacing';

const theme = {
  COLORS,
  FONTS,
  TYPOGRAPHY,
  SPACING,
  getSpacing,
  SHADOWS,
};

export {
  COLORS,
  FONTS,
  TYPOGRAPHY,
  SPACING,
  getSpacing,
  SHADOWS,
};

export default theme; 